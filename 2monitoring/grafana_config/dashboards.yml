apiVersion: 1

providers:
  - name: 'Prometheus'
    orgId: 1
    folder: 'Мониторинг'
    folderUid: 'monitoring'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 60
    options:
      path: /var/lib/grafana/dashboards
